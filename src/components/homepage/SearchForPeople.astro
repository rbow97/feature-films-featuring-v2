---

---

<input-wrapper class="flex justify-between w-full gap-6">
  <input
    autocomplete="off"
    id="text-input"
    placeholder="Search for someone..."
    type="text"
    class="bg-primary-lightGrey px-4 py-2.5 rounded-lg grow"
  />
  <button
    id="submit"
    class="rounded-lg px-4 py-2 bg-primary-lightGrey text-primary-grey"
    >Search</button
  >
</input-wrapper>

<script>
  import { handleInputSearchButton } from "../../utils/handleInputSearch";

  class InputWrapper extends HTMLElement {
    constructor() {
      super();
      const textInputString = "#text-input";
      const textInput: HTMLTextAreaElement =
        this.querySelector(textInputString)!;
      const submitButton = this.querySelector("#submit");

      textInput.addEventListener("keydown", function (e) {
        if (e.key == "Enter") {
          handleInputSearchButton((e.target as HTMLTextAreaElement).value);
        }
      });

      submitButton?.addEventListener("click", () => {
        handleInputSearchButton(
          (this.querySelector(textInputString) as HTMLTextAreaElement)?.value
        );
      });
    }
  }

  customElements.define("input-wrapper", InputWrapper);
</script>
