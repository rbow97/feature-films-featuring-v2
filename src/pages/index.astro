---
import DisplayResults from "@components/Display/DisplayResults";
import FloatingTagsList from "@components/FloatingTagsList/FloatingTagsList";
import Layout from "@layouts/Layout.astro";
import { getPopularPeople } from "src/api";

const people = await getPopularPeople();
---

<Layout title="Feature Films Featuring">
  <DisplayResults client:load popularPeople={people} />
</Layout>
